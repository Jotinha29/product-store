import { Component } from '@angular/core';
import { Routes } from '@angular/router';
import { LitsComponent } from './features/lits/lits.component';

export const routes: Routes = [{
    path: '',
    component: LitsComponent
}];
